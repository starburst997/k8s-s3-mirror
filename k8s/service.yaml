apiVersion: v1
kind: Service
metadata:
  name: s3-proxy
  namespace: s3-mirror
  labels:
    app: s3-proxy
spec:
  type: ClusterIP
  # Fixed ClusterIP for hostAliases usage
  clusterIP: 10.96.100.100
  selector:
    app: s3-proxy
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  sessionAffinity: None